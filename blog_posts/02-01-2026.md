# Blog Post 
## David Ross
## 02-01-2026
```rust
fn new_func() -> Result<(), Error> {
let new_var = 1 + 2
Ok(())
}
```

This is just some text. I'm testing to see if I can render markdown in a static way on my site. 
The idea would be to have boilerplate headers and then articles with links etc.
